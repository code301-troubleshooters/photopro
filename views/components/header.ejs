<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="./style/base.css">
</head>

<body>
    <header class="index-header">
        <nav>
            <div class="logo">
                <h1>PhotoPro Logo</h1>
            </div>
            <div class="nav-bar">
                <a class="nav-link" href="/">Home</a>
                <div class="nav-dropdown" id="navDropdown">
                    <a>Recommendations</a>
                    <div class="nav-dropdown-links hidden" id="navDropdownLinks">
                        <div class="inner-div">
                            <a class="nav-link" href="/courses">Courses</a>
                            <a class="nav-link" href="/books">Books</a>
                        </div>
                    </div>
                </div>
               
                <% if(!LoggedIn) {%>
                <a class="nav-link login-btn" href="/register">Register</a>
                <a class="nav-link login-btn" href="/login">Login</a>
                <% } else{%>
                <a class="nav-link">Welcome, <%=user.name%></a>
                <a class="nav-link" href="/favorite">Favorite List</a>
                <form action="/logout?_method=delete" method="POST">
                    <button class="nav-link login-btn" type="submit">Logout</button>
                </form>
                <% } %>
            </div>
        </nav>
    </header>